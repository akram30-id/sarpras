name: DEV Server HFO Sarpras Deploy
on:
  push:
    branches:
      - dev
  pull_request:
    branches:
      - dev

jobs: 
  FTP-Deploy-Action:
    name: FTP Deploy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: FTP Deploy
        # You may pin to the exact commit or the version.
        # uses: SamKirkland/FTP-Deploy-Action@8a24039354ee91000cb948cb4a1dbdf1a1b94a3c
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          # FTP host
          server: ftpupload.net
          # FTP user
          username: if0_36536625
          # FTP password
          password: TL9903hhKkR
          # Remote directory
          server-dir: /htdocs/
